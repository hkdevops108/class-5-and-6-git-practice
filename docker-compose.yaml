version: '4'

services:
  app:
    build: .
    container_name: sample_app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - DB_HOST=localhost
    volumes:
      - .:/app

